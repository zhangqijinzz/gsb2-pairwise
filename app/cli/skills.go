package cli

// builtinSkills maps skill directory name → SKILL.md content.
// These are always written on startup (overwriting any previous version).
// User customisations should be made in a separate fork of the skill directory.
var builtinSkills = map[string]string{
	"评审项目提示词生成": skillPromptGen,
}

// skillPromptGen is SKILL.md for the 评审项目提示词生成 skill (v3.1).
// v3.0 changes:
//   - Added output format anchoring at the top (输出铁律) for stronger constraint
//   - Step 3: changed from Agent sub-agents to direct Read for speed
//   - Step 5: stronger output prohibition with fallback rules
//   - Added ⛔ output prohibition section at the bottom
//
// v3.1 changes:
//   - Step 3: split read-depth by taskType; Bug 修复类不再受 2 文件上限
//   - New Step 3.5: Bug 定位与复现路径确认（调用链通读 / 候选清单 / 优先级排序 / 可复现性强校验）
//   - Step 5: 新增 Bug 修复类额外自检，防止"像 bug 但代码里不成立"的题目
//
// v3.2 changes:
//   - Bug 修复类按 scope 区分选题复杂度；多文件 scope 必须选择联动链路缺陷，避免单点小 bug
//
// v3.3 changes:
//   - 引入 project-requirement-generator 的真实项目压力、任务类型边界和难度判断规则
//
// v3.4 changes:
//   - 用可核查结果和真实边界替换旧的 80 字压缩规则
//   - 隔离评分/收录信息，禁止通过模糊或不可完成要求诱导失败
//
// v3.5 changes:
//   - 困难题增加联动链路和两类复杂度准入门槛，禁止把独立局部小修拼成困难题
//
// v3.6 changes:
//   - 难度下限抬到困难，只允许输出困难或地狱，禁止简单和一般需求
//   - 0-1 代码生成必须与现有系统深度衔接，不接受孤立脚手架或从零搭建
//   - 困难门槛由两类复杂度放宽为一类，允许同一业务模块内多文件联动
const skillPromptGen = `---
name: 评审项目提示词生成
description: 根据评审项目代码仓库自动生成评测提示词（未归类/Bug修复/0-1代码生成/Feature迭代/代码理解/代码重构/工程化）
---

# 评审项目提示词生成

## ⚠️ 输出铁律（最高优先级，贯穿全流程）

你的最终响应有且只有一个裸 JSON 对象。之前、之后、之外不允许有任何文字。
所有分析、推理、自检在内部完成，绝不输出给用户。
如果你发现自己开始写分析文字，立刻停下，只输出 JSON。
禁止输出任何 Markdown 标题、表格、代码块、分析段落。

## 参数预填充模式（PINRU 调用时使用）

当消息中包含 [PINRU] 标记时，直接从消息中解析以下参数，跳过 AskUserQuestion：

taskType / constraints / scope / notes（可选）

否则使用 AskUserQuestion 一次性问三个问题。

## 执行步骤

### Step 1：解析参数

[PINRU] 模式直接解析，非 PINRU 模式用 AskUserQuestion。

### Step 2：读取执行手册

根据 taskType 读取对应手册（必须完整阅读）：
- 未归类 → 不读取，直接结合仓库现状挑选
- Bug修复 → {{MANUAL_DIR}}/01_Bug修复任务执行手册.md
- 0-1代码生成 → {{MANUAL_DIR}}/02_代码生成任务执行手册.md
- Feature迭代 → {{MANUAL_DIR}}/03_Feature迭代任务执行手册.md
- 代码理解 → {{MANUAL_DIR}}/04_代码理解任务执行手册.md
- 代码重构 → {{MANUAL_DIR}}/05_代码重构任务执行手册.md
- 工程化 → {{MANUAL_DIR}}/06_工程化任务执行手册.md

### Step 3：快速扫描代码仓库

当前目录即为代码仓库。

1. 运行一次 find 获取目录结构：
find . -maxdepth 3 -not -path '*/.git/*' -not -path '*/node_modules/*' -not -path '*/__pycache__/*' | sort

2. 根据 taskType 决定读码深度：
   - 非 Bug 修复类：从目录结构判断最核心的 2 个业务文件，Read 顺序读取。
   - Bug 修复类：进入 Step 3.5，按调用链读码；除非已经发现候选不成立，否则不要无限扩散。

注意：Step 3 及 Step 3.5 的所有分析结果仅供内部使用，不要在最终响应中输出任何分析内容。

### Step 3.2：真实项目压力识别（所有 taskType 都必须执行）

目标：生成像真实项目排期里的研发任务，而不是泛泛产品想法或轻量待办。

内部识别以下信息，不输出：
- 项目已有的主流程、角色入口、页面组成、数据流和状态流。
- 异步更新、缓存、本地持久化、权限边界、导入导出、统计、预约、订单、通知、支付、库存、审核等容易产生联动问题的能力。
- 异常恢复、边界值、重复提交、跨页面返回、弱网失败、状态竞争、数据一致性、测试兜底等真实工程压力点。

任务生成必须贴合当前仓库已有能力。不要写“优化体验”“完善逻辑”“增强稳定性”这类空话。每个提示词必须自然写清当前情况、触发场景、目标行为和可核查的交付结果，并至少包含一个真实边界、异常场景或旧行为兼容要求。结果应能通过页面反馈、状态变化、数据结果、接口行为或测试产物确认。

审核信息与题目严格隔离：题目不得出现五维评分、21分收录门槛、审核通过率、压分或扣分暗示。不能故意制造失败、保留缺陷、设置不可完成条件，也不能用模糊要求诱导模型出错。只能基于项目真实需求和代码事实提高任务的区分度。

任务类型边界：
- 0-1代码生成：此前不存在的完整模块、新子系统或新主流程，且必须与现有角色、页面、数据流或业务链路深度衔接；不要把普通新增按钮、字段或局部配置写成 0-1，也不要写成空白项目的从零脚手架或只有增删改查的简单模块。
- Feature迭代：在已有功能基础上的平滑扩展，说明当前流程哪里不够、扩展后解决什么摩擦，并考虑旧行为兼容、异常场景或上下游影响。
- Bug修复：当前项目中真实存在或由代码迹象支撑的缺陷，优先跨模块、多状态、多边界；不要为了凑数编 Bug。
- 代码理解：聚焦某条核心链路、复杂状态流、异常分支或风险来源，必须有明确要回答的问题。
- 代码重构：业务结果不变，重点是性能、可读性、职责边界、重复逻辑或维护成本。
- 工程化：聚焦构建、依赖、发布、CI/CD、环境一致性或协作稳定性。
- 代码测试：围绕高风险流程、复杂规则、边界输入、异常恢复或回归风险补测试，不要只写补覆盖率。

### Step 3.5：Bug 定位与复现路径确认（仅 taskType = Bug 修复时执行）

目标：**先在代码里找到真实存在的 bug，再翻译成用户语言**，而不是先想一个像 bug 的故事再套到项目上。

1. 按"疑似 bug 关注点 → 相关调用链完整读完"的顺序读码：
   - 先挑一个聚焦点（例如某个用户动作、某个表单、某个状态持久化）。
   - 把该关注点涉及的**所有文件**都 Read 完整：入口视图/组件 + 相关 store/状态管理 + 相关 API 层 + 路由守卫/拦截器 + 涉及的后端处理（如果在仓内）。
   - 禁止只读 1-2 个文件就下结论。跨文件交互（store 清理、路由守卫、全局拦截器）是 bug 高发区，必须看全。
   - 读码上限：单文件 scope 最多读 3 个文件；模块内多文件最多读 5 个文件；跨模块多文件或跨系统多模块最多读 7 个文件。只有当前候选被证伪时，才换一个聚焦点继续读，不要为了穷举全仓而拖慢生成。

1.5. 按 scope 校准 bug 复杂度：
   - scope 包含"单文件"且不包含更大范围时，可以选择局部缺陷，但必须是真实存在、用户可观察的问题。
   - scope 包含"模块内多文件"时，候选 bug 必须需要同一业务模块内多个协作部分一起修复，例如页面交互、状态计算、数据读写、校验或展示链路不一致。
   - scope 包含"跨模块多文件"或"跨系统多模块"时，候选 bug 必须需要多文件、多层链路联动修复，优先找前后端契约、列表与详情、统计与导出、权限与操作入口、状态流转与库存/订单/通知、缓存与数据刷新这类跨模块不一致问题。
   - 多文件 scope 下，禁止选择只改一个判断、一个字段、一个按钮状态或一处文案就能修复的小 bug；如果当前关注点只找到这种小 bug，必须换关注点继续找。

2. 产出内部 bug 候选清单（不输出），每个候选必须能填完以下四项：
   - 触发路径：用户最少几步操作能触发（例如"输错密码 5 次 → 登出 → 刷新"）
   - 涉及代码：文件名 + 行号 + 相关函数
   - 根因：代码为什么会错
   - 可观察现象：用户在 UI/接口返回上能直接看到什么异常

3. 从候选里挑一条出题，**优先级排序**：
   - 触发路径 ≤ 3 步用户操作
   - 现象直观可见（UI 变化、报错、数据异常），不需要打开 devtools 才发现
   - 不依赖特殊前置状态（session 过期、脏 localStorage、并发等）
   - 如果所有候选都超过 3 步或依赖特殊前置状态 → 选最短那个，并且题目的"触发场景"必须把前置动作**逐步写全**，不得省略

4. 可复现性强校验（内部自问，任一不通过则换 bug 或改写触发场景）：
   - 新克隆仓库、干净环境下，按题目里写的步骤能不能看到题目里写的现象？
   - 题目描述的"当前行为"是不是代码真实行为？（不是想当然的 bug 故事）
   - 模型只读题目、不看我的分析，能不能按题目定位到同一块代码？

### Step 4：生成提示词

根据手册规范、代码分析、用户选择和 notes，生成一道评测提示词。

铁律（违反即重新生成）：
- 只写业务需求，读起来像产品经理在说话
- 零技术标识：文件路径、类名、函数名、变量名一律不出现
- 零 Markdown：无标题、无粗体、无代码块、无列表符号
- 完整适中：正文 3-6 句，写清当前情况、触发场景、目标行为和可核查的交付结果
- 字数限制：全文建议控制在 150-300 个字（空白字符不计入），不要为了压短而删掉真实边界和验收信息
- 真实边界：至少写入一个由代码事实支持的异常场景、边界条件或旧行为兼容要求，不堆砌无关复杂度
- 难度下限：只出困难或地狱的题目；若当前切入点只能做成单点小修、局部调整或从零脚手架，必须换题，禁止输出简单或一般难度的需求
- 审核隔离：不得出现五维评分、21分收录门槛、审核通过率、压分或扣分暗示，不能故意制造失败或诱导模型出错
- 约束融入正文：所有约束要求（技术栈、架构、代码风格、业务规则等）必须作为正文的自然组成部分写出，和需求描述合在同一段里，像开发者在聊天窗口一口气说完的话。严禁将约束单独分段、分行或加任何前缀标签。严禁出现"xx约束：""xx约束:""xx要求："等"标签名称：内容"形式的分类标头。若无约束则不加任何约束相关句子
- 修改范围：scope 覆盖面体现在题目的功能范围里
- 去 AI 味：
  1. 禁用词：此外、同时、另外、彰显、培养、突出、展示、至关重要、格局、持久、不可或缺、兜底、根因、收口、顺手、再顺一遍、收尾、落库
  2. 禁用结构："首先……其次……最后……""不仅……而且……""综上所述"以及机械套用的"通过 X 实现 Y"
  3. 禁用三段式列举
  4. 禁止过度限定："可能""或许""一定程度上"
  5. 句子节奏长短交错
  6. 读感测试：像真人抱怨问题就过，像说明书就重写

正面参考示例（约束融入正文，一整段写完）：
公益岗位有合同到期时间，现在系统只能查看所有在职记录，没法提前看到哪些合同快到期了。在公益岗位模块加一个到期预警视图，列出30天内要到期的岗位和已过期未退出的人员，标明剩余天数。使用现有桌面端 IPC 通信方式对接数据，不引入新的第三方包。查询逻辑放在主进程，渲染进程只负责展示，保持现有进程分工。对外函数需标注参数和返回类型，变量名采用小驼峰格式。

反面示例（严禁这样写——约束单独分行加标签前缀）：
❌ ...标明剩余天数。
技术栈约束：使用现有桌面端 IPC 通信方式对接数据，不引入新的第三方包。
架构约束：查询逻辑放在主进程，渲染进程只负责展示，保持现有进程分工。
代码风格约束：对外函数需标注参数和返回类型，变量名采用小驼峰格式。

### Step 5：自检、写入、结束

**自检**（内部完成，不写进响应）：逐项确认题目来自真实项目，当前情况与目标行为明确，包含可核查的交付结果和至少一个真实边界，与已有题目在业务目标和用户路径上明显不同，未泄露任何评分或收录信息，文字自然通顺且没有模板腔。任一项不满足都必须在内部重写后再输出。

**Bug 修复类额外自检**（任一不通过必须重写）：
- 题目里的"当前行为"是不是代码里真实存在的行为，而不是猜测？
- 题目里的"触发场景"是不是最短复现步骤的 1:1 翻译，前置动作（登录、登出、过期、刷新、切页）写全了吗？
- 被测模型按题目去代码里找，能不能定位到我在 Step 3.5 选定的那段代码？
- 如果 scope 是模块内多文件、跨模块多文件或跨系统多模块，这个 bug 是否必须改动多个协作文件才能修复？如果只改一个判断、字段、按钮状态或文案就能解决，必须换题。

**写入**：用 Write 工具把提示词正文写入当前目录的 任务提示词.md。

**难度判断**：根据最终提示词的真实实现成本给出 promptDifficulty，只能取 困难 或 地狱，下限为困难，不允许输出简单或一般。
- 困难：需要整合多个模块或系统约束，处理复杂状态、兼容性、权限、并发、性能、异常链路或测试兜底。
- 地狱：高耦合、高不确定、验证成本极高的少数情况。
- 判断时优先看理解成本、决策成本和约束复杂度，不要只按改动文件数量判断。
- 严禁简单需求：单文件局部改动、单个判断或字段调整、单个按钮或文案调整、只补一个校验或提示、只做样式微调这类题目不得输出；如果当前切入点只能做成这类小修，必须换一个有真实联动链路的题目重新出题。

**困难题准入门槛**：最终题目必须存在一条不能拆成互不影响的局部小修的联动链路，并至少命中以下一类真实复杂度：跨模块或跨层的数据与状态传递；状态机、异步时序、并发或失败恢复；多入口、持久化与展示之间的一致性；兼容旧数据或旧行为且需要成组回归验证。同一业务模块内多个协作部分的真实联动也可以构成困难题，不强求跨模块。独立的样式、提示或输入校验不能因为边界条件写得多就判为困难。文字截断与完整名称提示、本地存储失败提示、上传文件类型或大小校验都属于典型局部小修；把几项互不关联的小修拼在一起，也不能抬成困难题。达不到门槛时必须换一个有真实联动链路的题目重新出题，不得降级为简单或一般，不得虚构复杂度或硬贴困难标签。

**结束**：你的最终响应只能是这个裸 JSON：
{"version":1,"prompt":"提示词全文","promptDifficulty":"困难|地狱","artifactPath":"任务提示词.md绝对路径","fileWritten":true}

## ⛔ 输出禁令（违反即判定失败，无例外）

你调用完所有工具后，发送给用户的最终文本响应必须且只能是上面那个 JSON 对象。

绝对禁止：
- Markdown 标题（# ## ###）、表格、代码块
- JSON 前的任何文字（"生成结果"、"提示词如下"、"现在我已经"、"让我"、"基于"）
- JSON 后的任何补充
- 任何分析性内容（Bug 原因、涉及范围、架构说明）
- 以"我"开头的任何句子

**最后**：不确定是否该输出某段文字，就不要输出。宁可只有 JSON，不可多一个字。
`
